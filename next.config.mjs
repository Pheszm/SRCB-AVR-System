/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  trailingSlash: false, // This will remove the trailing slash on how it adds '/' in github
};

export default nextConfig;
